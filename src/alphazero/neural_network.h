

namespace aithena {
namespace alphazero {

class NeuralNetwork {
 public:
  NeuralNetwork() {}

  // Generate next state from neural network output:
  //
  // action = nn.evaluate(current_state)
  // next_board = gen_next_board(current_state, action)
  // next_state = current_state.GenNextStates().findByBoard(next_board)

 private:
};

}  // namespace alphazero
}  // namespace aithena